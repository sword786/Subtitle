// Safe ipcRenderer preload script. Keep blank if client only requires vanilla sandboxed modules.
window.addEventListener('DOMContentLoaded', () => {
  console.log('Captify AI native desktop sandbox preload script initialized.');
});
